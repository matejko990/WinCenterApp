Class Application

    ' Zdarzenia na poziomie aplikacji, takie jak Startup, Exit i DispatcherUnhandledException
    ' mogą być obsługiwane w tym pliku.

    'Sub Application_Startup(sender As Object, e As StartupEventArgs)

    '    Dim Main = New LoginScreen
    '    Main.Show()

    'End Sub

End Class
